"""
MedMatch: structured medication order formatting and LLM-based evaluation.

This repository exposes prompt builders under ``src`` and runnable pipelines under
``scripts``. Import the ``src`` package after adding the repository root to
``sys.path``, or run scripts from the repository root (they set the path
automatically).
"""

__version__ = "0.1.0"

__all__ = ["__version__"]
