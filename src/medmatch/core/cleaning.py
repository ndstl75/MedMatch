"""Compatibility layer for shared comparison and parsing helpers."""

from medmatch.core.scorer import (
    all_fields_match,
    coerce_output_object,
    compare_results,
    normalize_relaxed,
    normalize_strict,
    parse_json_response,
)

